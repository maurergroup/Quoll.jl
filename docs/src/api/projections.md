# Quoll.Projections

Core-basis projection schemes (`FC99V`, `LaikovCore`) and the pipeline
entry point [`perform_core_projection`](@ref).

```@meta
CurrentModule = Quoll.Projections
```

```@autodocs
Modules = [Quoll.Projections]
```
